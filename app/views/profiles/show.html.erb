<div class="container">
  <br>
  <div class="header mx-2">
    <h2>Saved Combos</h2>
  </div>
  <% if @user_combos.empty? %>
    <%= render 'no-saved-cards' %>
  <% else %>
    <%= render 'shared/combo-selection-cards' %>
  <% end %>
</div>
