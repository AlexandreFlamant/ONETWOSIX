const whitenArrow = () => {
  // const form = document.querySelector(".search");
  // form.addEventListener("submit", (event) => {
  //   const nextArrows = document.querySelectorAll(".slick-next");
  //   const prevArrows = document.querySelectorAll(".slick-prev");
  //   const carousel = document.querySelector(".carousel");
  //   if(prevArrows) {
  //     prevArrows.forEach((arrow) => {
  //       arrow.addEventListener("click", (event) => {
  //         arrow.remove();
  //         const arrowHTML = `<button class="slick-prev slick-arrow" aria-label="Previous" type="button" style=""></button>`;
  //         carousel.insertAdjacentHTML("afterbegin", arrowHTML);
  //       });
  //     })


  //     nextArrows.forEach((arrow) => {
  //       arrow.addEventListener("click", (event) => {
  //         console.log("here");

  //         arrow.remove();
  //         const arrowHTML = `<button class="slick-next slick-arrow" aria-label="Next" type="button" style=""></button>`;
  //         carousel.insertAdjacentHTML("beforeend", arrowHTML);
  //       });
  //     })
  //   }
  // })
}

export { whitenArrow }
